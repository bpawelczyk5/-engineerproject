<ui:FluentWindow
    x:Class="CerberPass.Windows.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CerberPass.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="MainWindow"
    Width="600"
    Height="800"
    d:DataContext="{d:DesignInstance local:LoginWindow,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="800"
    d:DesignWidth="600"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid>


        <ui:TitleBar Title="CerberPass" Height="35" />
        <ui:TextBlock
            Width="202"
            Margin="0,58,0,0"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            FontTypography="TitleLarge"
            Foreground="White"
            Text="CerberPass" />

        <ContentControl x:Name="LoginContent" Margin="52,35,52,8" />
    </Grid>
</ui:FluentWindow>
